@model SuperMarket_Models.Models.Brand_Category
@{
    ViewData["Title"] = "Create Brand";
}

<h3>@ViewData["Title"]</h3>
<div class="row">
    <div class="col-md-6">
        <p>@ViewBag.msg</p>
        <form id="profile-form" method="post" asp-action="CreateCategory">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input asp-for="BrandId" type="hidden"/>
            <div class="form-floating">
                <input asp-for="Brand.BrandName" class="form-control" />
                <label asp-for="Brand.BrandName" class="form-label"></label>
            </div>
            <div class="form-floating">
                <input asp-for="Brand.Origin" class="form-control" />
                <label asp-for="Brand.Origin" class="form-label"></label>
            </div>
            <div class="form-floating">
                <input asp-for="Brand.Address" class="form-control" />
                <label asp-for="Brand.Address" class="form-label"></label>
            </div>
            <div class="form-floating">
                <input asp-for="Brand.Phone" class="form-control" />
                <label asp-for="Brand.Phone" class="form-label"></label>
            </div>
            <div class="form-floating">
                <select class="form-control" asp-items="@ViewBag.categoryList">
                    <option value="test">Test</option>
                </select>
                <label asp-for="Category" class="form-label"></label>
            </div>
            <button id="update-profile-button" type="submit" class="w-100 btn btn-lg btn-primary">Create</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}

